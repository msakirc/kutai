# Handoff — [1.0c] prior_art_synthesize: dead/dormant mislabel (Rule 4) + result truncation

**Date:** 2026-06-21
**Status:** real failure, **two distinct sub-issues**. The gate is working correctly; the model output is the problem. Owner previously **deferred** the terminal case of this gate (see memory `project_prior_art_coverage_20260615` — "C terminal DEFERRED by owner").

## Symptom (live evidence)
Task **524364** `[1.0c] prior_art_synthesize`, mission 87:
- `status=failed`, `error_category=None`, `result` length 4743.
- `error = "prior art min coverage gate: unverifiable dead/dormant solutions (no Wayback + no HN ref): ['Habitica', 'Stre…]"`.
- The `result` JSON is **truncated mid-`key_lessons` array** (`json.loads` fails at char 4743 — the string just stops).

## Sub-issue A — the coverage gate is correct; the model mislabeled live products
The blocker is `prior_art_min_coverage` **Rule 4** (`packages/mr_roboto/src/mr_roboto/prior_art_min_coverage.py:185-199`):
> every `attempted_solutions` entry with `status in ("dead","dormant")` must carry `wayback_first_capture` OR an HN reference in `sources`.

The synthesizer marked **Habitica / Streaks / Loop** as dead/dormant with no Wayback/HN evidence. Those are **live, actively-maintained products** — the model misclassified them. The gate is doing exactly its anti-fabrication job. So this is **not a gate bug**; it is producer content quality.

Fix directions (decide with owner — this gate's terminal behavior was deliberately deferred):
1. **Prompt fix (preferred, low-risk):** tighten `[1.0c]` instruction (`i2p_v3.json:1283`) — "do NOT set status=dead/dormant unless you have a Wayback capture or HN reference; default to `active` when unsure." This removes the mislabel at the source.
2. **Retry feedback already exists:** the gate's `problems[]` ride back into the retry prompt. Verify the synthesizer actually receives "set status=active or attach Wayback" guidance on retry (it may just keep re-emitting the same dead labels — check the messages).
3. **Do NOT loosen Rule 4** — it exists to stop fabricated graveyards. Owner already ruled the terminal/true-zero case deferred; don't reopen without a policy decision.

## Sub-issue B — result truncated at 4743 chars
The `result` column JSON is cut off mid-array. Note: `prior_art_min_coverage` reads from the **file** (`report_path`), not the result column (`_load_report` prefers a non-empty inline report, else the file) — and it DID parse a report (it reached Rule 4), so the on-disk file was complete enough. The truncation is therefore in the stored `result`, likely cosmetic for THIS gate but a smell:
- Producer model for 1.0c (check `task_state.used_model` / `model_call_tokens`) — if it is a thinking model, this is the **thinking-token / context-overflow truncation class** (`project_thinking_token_budget`, `project_blackboard_truncation_fix_20260620`, `project_context_overflow_load_sizing_20260621` `2e23f861`). prior_art_synthesizer is a named victim of context-overflow.
- Action: confirm the model + whether `2e23f861` (restart-gated) resolves the truncation post-restart. If a stronger model is selected after cloud quota resets, both the mislabel and the truncation may clear together.

## Method
- `tasks.task_state.messages` for 524364 — see what the synthesizer was told vs emitted, and whether retry feedback steered it off the dead labels.
- `model_call_tokens` by task_id 524364 — model + prompt/completion sizes (truncation = completion hit a cap?).
- Inspect the on-disk report: `mission_87/.research/prior_art_report.json` — is it complete and does it really carry dead/dormant Habitica/Streaks/Loop?

## Files
- Gate: `packages/mr_roboto/src/mr_roboto/prior_art_min_coverage.py` (Rule 4 at :185).
- Step: `src/workflows/i2p/i2p_v3.json:1283` (`[1.0c]`, `post_hooks:["prior_art_min_coverage"]`).
- Related prior work: memory `project_prior_art_coverage_20260615` (relax-query + web-tier fallback A+B merged `360e4b8b`; terminal case C deferred).

## Sequencing
Re-pend after `/restart` first. If a healthy-cloud model produces correctly-labeled, untruncated output, this may be a storm artifact. Only invest in the prompt fix if the mislabel reproduces on a fresh, well-fed attempt.
