# Handoff — [1.11a] compliance_overlay: weak-model JSON-emission loop

**Date:** 2026-06-21
**Status:** real failure, **needs investigation** (not root-caused). Likely storm-linked model selection + small-thinking-model JSON emission. NOT a schema-gate contract bug.

## Symptom (live evidence)
Task **524377** `[1.11a] compliance_overlay`, mission 87:
- `status=failed`, `error_category=quality`, `result=NULL` (never produced parseable output).
- `worker_attempts=5` (near the cap), `grade_attempts=0`.
- `task_state.used_model = "openai/Qwen3.5-9B-UD-Q4_K_XL-thinking"` (a 9B **local** thinking model).
- `task_state.messages` (8 msgs) ends with the harness reply: **"Your response could not be parsed as valid JSON. Please fix the formatting and try again."** → a format-correction loop the model never escaped.
- The `error` column shows `Schema validation: … compliance_overlay.required_documents: empty placeholder value` — this is a **stale earlier-attempt** string; the live blocker is the unparseable-JSON loop, not the schema gate. (Result is NULL, so the gate never saw real content.)

## Why this step is hard for a 9B model
`[1.11a]` (`i2p_v3.json:1918`, difficulty=**medium**, agent=analyst) is operationally heavy: the instruction asks the model to iterate over every `<jurisdiction × data_category>` combo, call `compliance_template_render(...)` per doc, write each rendered file, surface `cross_jurisdiction_conflicts`, set per-doc review flags, and emit a structured `compliance_overlay` JSON with `required_documents` / `monitoring_obligations` / `data_subject_rights_implementation`. A 9B thinking model burns its budget on reasoning and emits malformed JSON.

## Hypotheses to test (in order)
1. **Storm-linked selection.** Was the 9B local model picked only because cloud was daily-exhausted during the overnight storm? Check `model_pick_log` for this task_id / time window and `model_call_tokens`. If yes, this likely self-resolves once cloud quota resets after `/restart` — re-pend and see what model gets assigned. (Don't fix selection if the storm was the cause; confirm first.)
2. **Thinking-token JSON budget.** Does the thinking model emit reasoning that crowds out / breaks the JSON block? Cross-reference the thinking-token-budget work (`project_thinking_token_budget`) and the context-overflow load-sizing fix (`project_context_overflow_load_sizing_20260621`, `2e23f861`) — `[1.11a]` analyst is in that fix's named victim set. Verify whether `2e23f861` (restart-gated) already helps.
3. **Difficulty mismatch.** `difficulty="medium"` may under-rank the step; a multi-call compliance step is closer to "hard". If selection keeps handing it to sub-9B-class models even with healthy cloud, consider bumping difficulty or adding a min-capability hint. Decide only after (1).
4. **Format-correction loop has no escape to a stronger model.** The 8-message loop burned worker_attempts without escalating model tier. Worth checking whether repeated JSON-parse failures should trigger a model escalation (HaLLederiz Kadir / Fatih Hoca selector feedback) rather than re-prompting the same weak model.

## Method
- `model_pick_log` + `model_call_tokens` by `task_id=524377` (tz: these tables are local UTC+3; `logs/*.jsonl` ts is UTC).
- `tasks.task_state.messages` for 524377 — read the full 8-message loop to see exactly what malformed output the model produced.
- Re-pend AFTER `/restart` (cloud quota reset + `2e23f861` loaded) and observe the newly-selected model + whether it parses.

## Files
- Step def: `src/workflows/i2p/i2p_v3.json:1918` (`[1.11a]`).
- Selection: `packages/fatih_hoca/` (scoring, difficulty→tier). Execution/JSON-parse loop: `packages/hallederiz_kadir/` + `packages/coulson/src/coulson/react.py` (final_answer parse + format-correction re-prompt).
- Quality/degenerate detection: `packages/dogru_mu_samet/`.

## Do NOT
Do not "fix" the schema gate for this — the gate never rejected real content here. The blocker is upstream (model can't emit parseable JSON). Confirm storm-vs-systemic before touching selection or difficulty.
