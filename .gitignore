node_modules/
.next/
.env
__pycache__/
*.pyc
.worktrees/
.claude/scheduled_tasks.lock

# Runtime + caches
logs/
*.log
.benchmark_cache/
.idea/
.vscode/
data/chroma/
data/chroma.bak.*/
data/*.db
data/*.db-shm
data/*.db-wal

# Production SQLite at repo root (path overridden via DB_PATH env)
/kutai.db
/kutai.db-shm
/kutai.db-wal

# Legacy dead-models JSON state (migrated to SQLite registry, Step 5b)
# .archived kept as 1-week rollback safety net by operator; never commit
.dead_models.json
.dead_models.json.*

# Build / packaging artifacts
*.egg-info/
dist/
build/

# Mission workspaces (runtime output — never commit)
workspace/
src/workspace/
src/app/workspace/
frontend_scaffold/
backend_5.0.0/

# Leaked Next.js scaffold at project root (should never exist here)
/package.json
/package-lock.json
/tsconfig.json
/src/app/layout.tsx
/src/app/page.tsx
