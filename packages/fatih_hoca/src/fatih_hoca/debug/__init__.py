"""fatih_hoca.debug — diagnostic CLIs for the selection pipeline.

Modules:
    pressure_dump  — full per-signal breakdown for every (model, task)
    signal_check   — verify each signal can fire non-zero in trigger state
    matrix_audit   — dump RateLimitMatrix cell population per provider
"""
