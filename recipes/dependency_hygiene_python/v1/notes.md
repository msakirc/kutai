# dependency_hygiene (python)

V1 wraps `pip-audit --format json`. Patch-version auto-merge is gated by
`AUTO_MERGE_PATCH` and stays off in V1 — minor/major surface to founder
via `founder_actions` rows (T5B follow-up).
