# Handoff — 1.4a `competitive_positioning` `max_iterations_reached`

**Date:** 2026-06-21
**Status:** OPEN, NOT yet diagnosed to a single root. NOT currently live-reproducible (the original task is gone — see below). One strong upstream suspect already fixed this session; **observe before building**.
**Predecessor:** `docs/handoff/2026-06-21-analyst-no-candidates-resolution-handoff.md` §P2. Memory: `project_context_overflow_load_sizing_20260621`, `project_p1_boot_warmup_not_a_bug_20260621`, `project_checkpoint_freezes_reqs_20260620`.

---

## Symptom

During the analyst-no-candidates hunt, once mission 86 step **1.4a `competitive_positioning_lock`** (task 459160) was manually unblocked, it stopped failing at *selection* and started failing *downstream*: the analyst picks a model (gemini/gemma) that **executes successfully**, but the ReAct loop can't produce a valid final answer within the iteration budget → `max_iterations_reached`.

## Current state — NOT live-reproducible right now

- Task 459160 is **gone** from `tasks` (swept/resolved). 
- `max_iterations` is **not** a `model_pick_log` `error_category` (that table is per-*call*; every call succeeded). 0 rows in pick_log / tasks for `%iter%` as of 2026-06-21.
- So there is **no standing victim to watch**. Repro must be manufactured (see Repro plan).

## Mechanism (traced, exact)

1. `react.py:427` `for iteration in range(effective_max_iterations)`. For **analyst, `max_iterations = 5`** (`packages/finch/src/finch/profiles/analyst.yaml:5`; default is 10, analyst is capped low). Boost path: `iteration_budget_boost` can lift it to ≤12 (`react.py:358-361`); `quality_mode` quick→1, thorough→5 (`react.py:367-384`).
2. Loop ends without a `final_answer` → react returns `status="exhausted"` (`react.py:~1866`, `useful_iterations/effective_max_iterations` logged).
3. `general_beckman/result_router.py:221-226` → `Exhausted(error="max_iterations_reached")`.
4. `apply.py::_apply_exhausted` → `_retry_or_dlq(category="exhausted")`.
5. **`"exhausted"` is NOT in `TRANSIENT_CATEGORIES` and NOT `"quality"`** (`retry.py:40-43`). In `decide_retry` it takes the exponential-backoff branch but with the **RAW** `max_worker_attempts` cap (transient gets the cap extended to the 15-step ladder; exhausted does not). So it retries on backoff, re-runs the analyst (5 fresh iterations each), **re-exhausts deterministically**, and DLQs at the raw cap.

This is the "quality/complexity" class: same inputs + same model tier → same failure to finalize. Backing off doesn't help.

## Why 1.4a specifically is hard (the artifact contract)

`src/workflows/i2p/i2p_v3.json:1481` (step `1.4a`). The analyst must **synthesize 3 upstream artifacts** — `direct_competitors_list` (1.3), `indirect_competitors_list` (1.4), `product_charter` — into `mission_{id}/.prd/competitive_positioning.md` with a **strict exact shape**:

- YAML frontmatter: `_schema_version`, `mission_id`, **non-empty `named_competitors`**.
- **6 sections**: Landscape, Value Thesis, Strengths/Weaknesses, Our Differentiators, Switching Costs & Risks, Notes.
- **Cross-reference rules**: every `named_competitors` entry must appear in Landscape + Value Thesis + Strengths/Weaknesses; **Differentiators must cite ≥1 charter Solution name verbatim**; Switching Costs must be a real paragraph (single-line stub fails); no placeholder text.
- **Two gates**: mechanical post-hook `1.4a.verify` (`verify_competitive_positioning_shape`) rejects malformed shape; the **1.13 reviewer** rejects if differentiators don't cite charter solutions.
- `context.estimated_output_tokens: 4000`.

A multi-input synthesis with a 6-section schema + cross-refs + verbatim-citation requirement is genuinely tight for **5 iterations** on a mid/local model. If the model emits a near-miss (a section short, a competitor missing from one section, a stub paragraph), verify rejects, the loop iterates, and 5 runs out.

## STRONG upstream suspect — likely already mitigated this session

`context_overflow` was fixed in **`2e23f861`** (`_resolve_load_ctx` now sizes the local load to the live prompt, not the frozen admission `need_ctx` — see `project_context_overflow_load_sizing_20260621`). 1.4a is a **deep-context** task (3 upstream artifacts + charter injected). If the prompt was overflowing/truncating the small-loaded local model mid-iteration, the model saw a **garbage/truncated prompt** → produced an invalid artifact → verify rejected → burned iterations → `max_iterations_reached`. 

**→ First action: re-run 1.4a AFTER `2e23f861` is live (restart) and see whether max_iterations persists.** It may be substantially or fully resolved. Do NOT build a fix until this is confirmed still-broken on a warm, post-fix run.

## Candidate fixes (if still broken post-overflow-fix — owner decides, do NOT pick blindly)

1. **Iteration budget** — bump analyst `max_iterations` (5→7?) OR set `iteration_budget_boost` / `quality_mode=thorough` for high-constraint synthesis steps. Cheapest; risk = just delays the DLQ if the model fundamentally can't satisfy the contract.
2. **Output-token budget** — `estimated_output_tokens: 4000` may cap the 6-section/multi-competitor artifact mid-list (same failure class as mission-81 ADR truncation: capped output → missing required section → verify reject). Bump for this step (clamp ≤16000 already supported via `requirements_builder` `_out_override`). Measure the real artifact length first.
3. **Difficulty routing** — route 1.4a to a stronger/cloud model (raise step `difficulty`) so it finalizes within 5. The selector already does difficulty→model tiering.
4. **Retry-hint precision** — ensure the `1.4a.verify` rejection feeds a **specific** retry hint (which section/rule failed) into the next attempt's context, so the analyst *fixes the named gap* instead of blindly re-synthesizing. Check whether `verify_competitive_positioning_shape` surfaces a per-rule reason and whether it reaches the retry prompt (hooks.py `_schema_error` plumbing).
5. **Scaffolded emit** — give the analyst a pre-filled section skeleton / schema-guided emit so a small model only fills content, not structure. Biggest change; highest reliability.

## Repro plan (manufacture a victim)

- Re-run an i2p_v3 mission to step 1.4a (needs 1.3 + 1.4 + charter populated), OR craft a standalone `analyst` task whose context carries `direct_competitors_list` + `indirect_competitors_list` + `product_charter` and the 1.4a instruction, then watch `useful_iterations/effective_max_iterations` in `orchestrator.jsonl` + the emitted `.prd/competitive_positioning.md` + the verify verdict.
- **Force a WARM retry** on any stuck instance — reset `next_retry_at=now, worker_attempts=0` (the long-backoff-hides-fixes lesson; an `exhausted` task on a deep ladder won't re-run for hours).
- Compare iterations + artifact validity **before vs after `2e23f861`** to isolate the overflow contribution.

## Forensics queries (prod DB `C:/Users/sakir/ai/kutai/kutai.db`, UTC)

```sql
-- exhausted/DLQ for analyst competitive_positioning:
SELECT id,status,worker_attempts,error_category,substr(error,1,80)
FROM tasks WHERE agent_type='analyst' AND error LIKE '%iteration%' ORDER BY id DESC;
-- per-run useful vs budget is in logs, not DB: grep orchestrator.jsonl for
--   "useful=" and "max_iterations" around the task id.
```

## Key files

- `packages/coulson/src/coulson/react.py` — `:358-384` budget (boost/quality_mode), `:427` loop, `:~1866` exhausted return.
- `packages/finch/src/finch/profiles/analyst.yaml:5` — `max_iterations: 5`.
- `packages/general_beckman/src/general_beckman/result_router.py:221-226` — `Exhausted("max_iterations_reached")`.
- `packages/general_beckman/src/general_beckman/{apply.py::_apply_exhausted, retry.py::decide_retry, retry.py:40-43 TRANSIENT_CATEGORIES}` — exhausted retry/DLQ path (RAW cap, not the 15-ladder).
- `src/workflows/i2p/i2p_v3.json:1481-1535` — 1.4a step: instruction, `artifact_schema`, `estimated_output_tokens`, `1.4a.verify` check.
- `packages/fatih_hoca/src/fatih_hoca/requirements_builder.py:223-278` — `estimated_output_tokens` `_out_override` (clamp [500,16000]).
- The verify executor `verify_competitive_positioning_shape` (mr_roboto) — for retry-hint precision (candidate 4).

## Gotchas

- **Observe before building.** The overflow fix (`2e23f861`) is a real upstream cause; a post-restart warm run may already pass. Don't fix a symptom that's gone.
- `exhausted` ≠ transient: it DLQs at the **raw** worker_attempts cap, and re-runs re-exhaust deterministically — so a budget bump alone may only move the DLQ, not prevent it.
- Long backoff ladders hide fixes — force a warm retry to validate, don't infer.
- Likely **linked to other downstream P2** (5.0a was the context_overflow one, now fixed; 1.4a may share the cause).
- Windows: never `run_in_background` pytest (orphans hold the prod SQLite lock); foreground + `timeout`; pkg tests `-p no:aiohttp -o addopts=""`.
- Restart-gated: `2e23f861` is on `main`, NOT pushed — must `/restart` (or full relaunch) before any post-fix observation is valid.
